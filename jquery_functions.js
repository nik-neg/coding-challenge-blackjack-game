$("h1").css("color", "red");

// addClass

// removeClass

// text

// html

// attr -> getter

/* click(function() { // adding a click listener

})
*/

// targeting input via keypress function(event)

$(document).keypress(function(event) {
    $("h1").text(event.key);
})

// adding new elements before/after an element with
// before/after
// prepend => between before
// append  => between after


// hide

// show

// toggle

// fadeout fadein

// slideup, slidedown, slidetoggle

// animate -> for custom css rules

// left to right order chaining

// effects